<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.dailypick.mapper.UserMapper">
    <resultMap id="UserMap" type="com.project.dailypick.dto.UserDto">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="password" column="password"/>
        <result property="userName" column="user_name"/>
        <result property="phoneNumber" column="phone_number"/>
        <result property="email" column="email"/>
        <result property="isSubscribe" column="is_subscribe"/>
        <result property="createDate" column="create_date"/>
    </resultMap>

    <select id="getUserById" parameterType="long" resultMap="UserMap">
        SELECT *
        FROM user
        WHERE id = #{id}
    </select>

    <select id="getUserByUserId" parameterType="string" resultMap="UserMap">
        SELECT *
        FROM user
        WHERE user_id = #{userId}
    </select>

    <insert id="createUser" parameterType="com.project.dailypick.dto.UserDto" useGeneratedKeys="true" keyProperty="id">
        INSERT user
        SET user_id = #{userId},
            password = #{password},
            user_name = #{userName},
            phone_number = #{phoneNumber},
            email = #{email},
            <choose>
                <when test="isSubscribe != null">is_subscribe = #{isSubscribe},</when>
                <otherwise>is_subscribe = 'N',</otherwise>
            </choose>
            create_date = NOW()
    </insert>
</mapper>